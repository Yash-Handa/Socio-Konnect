/**
 * exported object has all the configurational options
 * like all the `secrets` present in the `.env` file which
 * are relevant **only** for `testing` environment
 *
 * like testing DB info, third party api keys used in testing
 * and other testing related secrets that are requires in the app
 *
 * all key value pairs in the exported object will override the config object's keys in config.js
*/

module.exports = {

};
